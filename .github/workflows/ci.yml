name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4 # Atualizado para v4
      - name: Use Node.js
        uses: actions/setup-node@v2 # Atualizado para v2
        with:
          node-version: "14" # Especifique a versão desejada do Node.js, por exemplo, "14"
      - run: npm install
      - run: npm test
